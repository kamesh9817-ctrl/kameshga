import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Read size of array
        int N = sc.nextInt();

        // Create original array
        int[] original = new int[N];

        // Read elements into original array
        for (int i = 0; i < N; i++) {
            original[i] = sc.nextInt();
        }

        // Create copied array
        int[] copied = new int[N];

        // Copy elements
        for (int i = 0; i < N; i++) {
            copied[i] = original[i];
        }

        // Print copied array
        for (int i = 0; i < N; i++) {
            System.out.print(copied[i] + " ");
        }

        sc.close();
    }
}
