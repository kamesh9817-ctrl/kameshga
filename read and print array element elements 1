import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int N = sc.nextInt();  // Read number of elements
        int[] arr = new int[N];

        // Read array elements
        for (int i = 0; i < N; i++) {
            arr[i] = sc.nextInt();
        }

        // Print array elements in same sequence
        for (int i = 0; i < N; i++) {
            System.out.print(arr[i] + " ");
        }

        sc.close();
    }
}
